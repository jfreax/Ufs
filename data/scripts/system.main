function include ( filename )
  -- write report
  io.write( string.format( "Loading scriptfile \"" .. filename .. "\" ..." ) );

  dofile ( getPath() .. filename );

  print ( "\t ok" );
end

include ( "system.functions" )

map_functions()